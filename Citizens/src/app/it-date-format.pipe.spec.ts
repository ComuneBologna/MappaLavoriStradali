import { ItDateFormatPipe } from './it-date-format.pipe';

describe('ItDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ItDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
